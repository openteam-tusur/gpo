<% if current_user %>
  <span class='login'><%= current_user %></span>
  <%= link_to 'Выход', sign_out_url, :method => :delete, :class => :logout %>
<% else %>
  <%= link_to 'Вход в систему', sign_in_url, :class => :login  %>
<% end %>

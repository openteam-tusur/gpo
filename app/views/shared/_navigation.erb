<ul id="header-navigation">
  <%= nav_link link_to("Моя страница", dashboard_url), 'sessions', :action => 'dashboard' if logged_in? %>
  <%= nav_link link_to("Кафедры", chairs_url), 'chairs' if permitted_to?(:index, :chairs) %>
  <%= nav_link link_to("Пользователи", users_url), 'users' if permitted_to?(:index, :users) %>
  <%#= nav_link link_to("Правила", rules_url), 'rules' if permitted_to?(:index, :rules) %>
  <%= nav_link link_to("Отчеты", reports_url), 'reports' if permitted_to?(:index, :reports) %>
  <%= nav_link link_to("Настройки", themes_url), 'themes' if permitted_to?(:index, :themes) %>
</ul>


<% if current_user %>
  <span class='login'><%= current_user.name %></span>
  <%= link_to 'Выход', '/sign_out', :class => :logout %>
<% else %>
  <%= link_to 'Вход в систему', user_omniauth_authorize_path(:identity), :class => :login  %>
<% end %>

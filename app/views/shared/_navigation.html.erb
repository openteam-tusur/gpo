<ul id="header-navigation">
  <%= nav_link link_to("Главная", root_path), 'sessions', :action => 'dashboard' %>
  <%= nav_link link_to("Управление", manage_root_path), 'manage', :action => 'dashboard', :selected => current_page?(manage_dashboard_path) || current_page?(manage_root_path) if can?(:manage, :application)%>
  <% if can?(:manage, :all) %>
    <%= nav_link link_to("Кафедры", manage_chairs_path), 'chairs' %>
    <%= nav_link link_to("Пользователи", manage_users_path), 'users' %>
    <%= nav_link link_to("Отчеты", manage_reports_path), 'reports' %>
    <%= nav_link link_to("Настройки", manage_themes_path), 'themes' %>
  <% end %>
</ul>


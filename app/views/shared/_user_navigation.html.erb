<ul>
  <%= nav_link link_to("Пользователи", manage_users_path), 'users' if permitted_to?(:index, :users) %>
  <%= nav_link link_to("Правила", rules_path), 'rules' if permitted_to?(:index, :rules) %>
</ul>


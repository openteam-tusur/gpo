<% content_for :title, "Закрытие проекта" %>

<h1><%= @project.cipher %> <%= @project.title %></h1>

<% unless @project.managers.empty? %>
  <h2>Будут удалены руководители проекта:</h2>
  <ul>
    <% @project.managers.each do |manager| %>
      <li><%= manager.text_for_order_report %></li>
    <% end %>
  </ul>
<% end %>

Вы уверены что хотите закрыть проект?

<%= simple_form_for :project, :url => close_chair_project_path(@project.chair, @project), :html => { :method => :put } do | form | %>
  <fieldset>
    <%= form.input :close_reason %>
    <%= form.button :submit, I18n.t("project.close") %>
  </fieldset>
<% end %>
<%= link_to I18n.t("cancel"), chair_project_path(@project.chair, @project) %>

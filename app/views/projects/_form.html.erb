<%= form.error_messages %>

<%= form.text_field(:title) if permitted_to?(:rename, form.object) %>
<%= form.select(:theme_id, Theme.find(:all, :order => "name").collect {|theme| [theme.name, theme.id]}, {:include_blank => true}) %>
<%= form.text_area :goal %>
<%= form.text_area :stakeholders %>
<%= form.text_area :funds_required %>
<%= form.text_area :funds_sources %>
<%= form.text_area :purpose %>
<%= form.text_area :features %>
<%= form.text_area :analysis %>
<%= form.text_area :novelty %>
<%= form.text_area :expected_results %>
<%= form.text_area :release_cost %>
<%= form.text_area :forecast %>
<%= form.text_area :source_data %>


<h3>График работы групп ГПО в <%= XlsReport.new.semestr %> семестре <%=XlsReport.new.edu_years %> уч. год. </h3>
<div class='issues'>
  <%= form_tag manage_report_update_multiple_path(chair: @chair), method: :put do %>
    <table>
      <thead>
        <tr>
          <th>№ группы ГПО</th>
          <th>ФИО руководителя</th>
          <th>Аудитория</th>
          <th>Время занятий</th>
        </tr>
      </thead>
      <tbody>
        <% @projects.each do |project| %>
          <%= fields_for "projects[]", project do |f| %>
            <tr>
              <td>
                <%= project.cipher %>
              </td>
              <td>
                <%=  project.people.collect {|person| person}.join(", ") %>
              </td>
              <td>
                <div class="field">
                  <%= f.text_field :auditorium%>
                </div>
              </td>
              <td>
                <div class="field">
                  <%= f.text_field :class_time%>
                </div>
              </td>
            </tr>
          <% end %>
        <% end %>
      </tbody>
    </table>
    <div class="actions">
    <%= submit_tag "Сохранить" %>
    </div>
  <% end %>
</div>

<% title "Изменение приказа #{order_title(@order)}", :order %>

<% content_for :hint do %>
  <h3>Изменение приказа</h3>
  <% if @order.approved? %>
    <p>Для изменения приказа исправьте значения полей формы  и нажмите "Сохранить".</p>
    <p>Поля номер и дата приказа являются обязательными для заполнения.</p>
  <% else %>
    <p>Для изменения приказа, отметьте проекты, состояние которых будет утверждено приказом и нажмите "Сохранить".</p>
  <% end %>
    <p>Если нет необходимости изменять приказ "Отмена" для возврата к просмотру приказа.</p>
<% end %>

<p>Выберите проекты, состояние которых будет отверждено приказом.</p>
<% form_for(:order, @order, :url => chair_order_url(@chair, @order), :html => {:method => :put}) do |form| %>
  <fieldset>
    <%= render :partial => 'form', :locals => {:form => form, :order => @order} %>
    <p>
      <%= form.submit l(:save) %>
      <%= link_to l(:cancel), chair_order_path(@chair, @order) %>
    </p>
  </fieldset>
<% end %>


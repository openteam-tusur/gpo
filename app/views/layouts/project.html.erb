<!DOCTYPE html PUBLIC "-//W3C//DTD XHTML 1.0 Transitional//EN"
  "http://www.w3.org/TR/xhtml1/DTD/xhtml1-transitional.dtd">

<html xmlns="http://www.w3.org/1999/xhtml" xml:lang="ru" lang="ru">
<head>
  <meta http-equiv="content-type" content="text/html;charset=UTF-8" />
  <title><%= @project.cipher %> <%=h @project.title %> &#x2014; <%= yield(:title) %></title>
  <%= csrf_meta_tags %>
  <%= render :partial => 'shared/css_and_js' %>
</head>
<body>
  <div id="header">
    <div id="header-wrapper">
      <%= render :partial => "shared/header" %>
    </div>
  </div>

  <div id="wrapper" class="project-level">
    <div id="navigation">
      <ul>
        <%= nav_link link_to(chair_abbr(@project.chair), chair_url(@project.chair)), 'chairs', :action => 'show', :selected => false if permitted_to?(:read, @project.chair) %>
        <%= nav_link link_to("Проекты", chair_projects_url(@project.chair)), 'projects', :selected => true if permitted_to?(:read, @project.chair) %>
        <%= nav_link link_to("Приказы", chair_orders_url(@project.chair)), 'orders', :selected => false if permitted_to?(:read, @project.chair) %>
        <%= nav_link link_to("Пользователи", chair_users_url(@project.chair)), 'users', :selected => false if permitted_to?(:chair_manage, @project.chair) %>
        <%= nav_link link_to("Руководители", managers_chair_url(@project.chair)), 'chairs', :action => 'managers', :selected => false if permitted_to?(:read, @project.chair) %>
        <%= nav_link link_to("Посещаемость", chair_visitations_url(@project.chair)), 'visitations', :selected => false if permitted_to?(:read, @project.chair) %>
      </ul>
    </div>

    <div id="content">
      <%= render :partial => 'shared/help' %>

      <div id="page-header">
        <div id="tray"></div>
        <div id="page-title">
          <h1><%= iconed_title("Проект #{@project.cipher}", :project) %><span class="subtitle"><%=h @project.title %></span></h1>
        </div>
        <div id="local-navigation">
          <ul>
            <%= nav_link link_to("Тематическая карта", chair_project_url(@project.chair, @project)), 'projects', :action => 'show' if permitted_to?(:read, @project)%>
            <%= nav_link link_to("Календарный план", chair_project_stages_path(@project.chair, @project)), 'stages' if permitted_to?(:read, @project) %>
            <%= nav_link link_to("Участники", chair_project_participants_path(@project.chair, @project)), 'participants' if permitted_to?(:read, @project) %>
            <%= nav_link link_to("Руководители", chair_project_managers_path(@project.chair, @project)), 'managers' if permitted_to?(:read, @project) %>
            <%= nav_link link_to("Посещаемость", chair_project_visitations_path(@project.chair, @project)), 'visitations' if permitted_to?(:read, @project) %>
            <%= nav_link link_to("Индивидуальные задачи", chair_project_issues_path(@project.chair, @project)), 'issues' if permitted_to?(:read, @project) %>
            <%= nav_link link_to("Приказы", chair_project_orders_url(@project.chair, @project)), 'orders' if permitted_to?(:read, @project) %>
          </ul>
        </div>
      </div>
      <%= render :partial => "shared/#{include_container}" %>
    </div>

    <div id="footer">
      <%= render :partial => 'shared/footer' %>
    </div>
  </div>
</body>
</html>


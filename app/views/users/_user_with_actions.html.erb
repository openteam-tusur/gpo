<%= render :partial => 'user', :object => object %>
<div class="actions">
  <%= link_to_edit_user(object, @chair) if permitted_to?(:update, object) %>
  <%= link_to_delete_user(object, @chair) if permitted_to?(:delete, object) %>
</div>

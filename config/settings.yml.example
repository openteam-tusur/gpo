app:
  secret:               secret

solr:
  url:                  http://localhost:8982/solr

errors:
  key:                  api_key
  host:                 host

storage:
  url:                  http://storage.host

deploy:
   gateway:             user@host
   application:         appname
   domain:              user@host
